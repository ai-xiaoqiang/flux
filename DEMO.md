# ⌛ 时光 Flux · 3 分钟演示脚本

> 一份照着走就能把平台能力全亮出来的演示流水线。
> 约 3 分钟，覆盖：**Agent 逛代码 → 拾光抓网页 → 知识库 RAG → 权限审批 → 会话持久化**。

---

## 准备（30 秒）

```bash
cd ~/Desktop/learn/AI/flux
npm run build && flux web        # 启动网页版
# 浏览器打开 http://localhost:8787
```
> 先点侧边栏 **＋新建对话**，清干净。

---

## ① Agent · 会逛代码库（40 秒）

**网页里发**：
```
分析一下这个项目，讲讲架构
```

**你会看到**：
```
▸ 🔧 工具过程 · N 步        ← 点开能看它怎么扫的
    list_files   ← 先看项目结构
    read_file    ← 再精读关键文件
📝 时光 Flux 是一个 AI Agent 平台…（分层回答）
```

**解说**：它不像新手那样瞎翻文件，而是"先看结构 → 再精读"——这就是 Claude Code 的读代码方式，我们自己实现的。

---

## ② 拾光 · 抓网页（30 秒）

**侧边栏 🕰 拾光 → 抓取网址**，粘贴一个真实网页，点抓取：
```
https://github.com/cystanford/wucai-code     （或任何技术文章）
```
**你会看到**：卡片 `📄 标题` + 正文进对话 → 点 **✨ AI 识别** 自动定分类 → 点 **📥 存入知识库**。

**解说**：抓取 → AI 自动分类 → 存库，一条龙。它读完网页还能直接答你问题。

---

## ③ 知识库 · 基于"你存的资料"回答（40 秒）

**网页里发**（问它自己刚才存进去的东西）：
```
根据我知识库里存的内容，说说这个项目（wucai-code）是什么
```

**你会看到**：
```
▸ 🔧 工具过程
    search_kb   ← 向量检索 + rerank 精排
📝 准确回答（基于你存的那份网页，不是凭空编）
```

**解说**：这是 **RAG**——存的文档切块→向量化落库；提问时语义检索(向量粗召回) + **rerank 精排**，拿最准的内容回答。数据全在你本机。

---

## ④ 权限 · 危险操作要你同意（30 秒）

**⚙ 设置 → 权限模式 → 切到「严格」**，网页里发：
```
帮我创建一个 test.txt，里面写 hello
```
**你会看到**：弹出审批框 → 点 **🚫 拒绝** → 时光机如实说没做成；文件没被创建。

**解说**：三档权限（完全/均衡/严格）× 工具风险，审批做成可插拔钩子——网页弹窗、终端 y/n 两处通用。

> 演示完切回 **完全** 模式，免得影响后面。

---

## ⑤ 会话持久化 · 重启还能接着聊（20 秒）

**终端里** `Ctrl+C` 停掉 flux web，再启动：
```bash
flux web        # 会提示「已恢复上次对话」
```
刷新网页，**直接问**（不用重新介绍）：
```
你还记得刚才我们聊了什么吗？
```
它记得——**对话历史落盘了**。

---

## 收尾（可选 · 讲给面试官一句话）

> "我自研了「时光 Flux」：一个 AI 时代的时光机。核心是一个事件驱动的 Agent 引擎（主循环+工具注册表+双记忆+可插拔审批），上面搭了 Web 界面、语音输入、文档解析、以及一套带 rerank 的本地知识库 RAG——抓取的资料存库后可语义检索问答。全部数据在本地，重启可续聊。"
