# 时光 Flux — 演进路线图

> 状态：草稿 v1（作者与 Claude 对齐后固化）
> 目的：把「对标完整 Agent」的目标拆成可落地、每个都**能独立验收**的里程碑，作为后续所有实现的共同基准。

---

## 1. 项目定位与设计准则

一句话：**Flux 是一个跑在本地 Mac 上、个人日常写代码/读仓库用的 AI Agent「壳」**——先只做通用 Agent 能力，再按需长工具。

> 名字「时光机 / 自由穿梭」= 能在任意代码库、任务、历史间来回的能力，这是终态气质，不是 M0 现状。

五条**设计准则**（由作者拍板，任何里程碑改动不得违反）：

| # | 准则 | 含义 |
|---|------|------|
| D1 | **脑壳分离，协议兼容** | 「壳」（循环/工具/记忆）与「脑」（模型）通过 **OpenAI 兼容 function-calling 协议**通信；模型/地址全部可配（`config.ts` 已支持 baseUrl/model 覆盖）。现在默认 DashScope qwen3-coder-plus，将来可切 DeepSeek/OpenAI/本地 vLLM，改动只在一个适配点。 |
| D2 | **自研优先，wucai 仅灵感** | 参考实现 wucai 只借思路，不逐行对齐；架构以自研为准，保持小而清晰。 |
| D3 | **代码优先** | 工具面第一优先服务「读仓库/改代码」：文件读写、搜索、跑编译/测试。其余（云端运维、数据库…）以后按需挂。 |
| D4 | **渐进对标完整 Agent** | 终态 = 多轮工具循环 + 可信的改码闭环 + 跨会话记忆 + 可打断的连续执行。每期都落在「现在能真用」的台阶上，不憋大招。 |
| D5 | **个人工具** | 成本与体感优先：默认收敛的步数/输出上限、清晰的终端进度、友好的错误；不追求多用户/高并发。 |

---

## 2. 现状盘点（M0，已完成）

能力：单轮问答。`flux "话"` 或 `echo 话 | flux` → 打印模型回复。

结构（4 个文件，很薄，好改）：

```
src/
  cli.ts    入口：读入→拼消息→问模型→打印（main + readInput）
  config.ts 配置：~/.flux/config.json + 环境变量覆盖（apiKey/baseUrl/model/cwd）
  prompt.ts 人设拼装（buildSystemPrompt / buildMessages）
  model.ts  FluxModel：openai SDK 打 DashScope，单轮 chat；已解析 tool_calls 但未执行
```

对下一步有利的**现有资产**：

- `config.ts` 已把 `baseUrl`/`model` 做成可覆盖 → D1 的配置层已就位。
- `model.ts` 已解析 `functionCalls`（`ModelResponse` 里留了位）→ M1 只差「把解析结果跑起来」。
- `prompt.ts` 已把「模型失忆 → 人设=壳拼的文字」想清楚 → M3 记忆注入点已预留（注释在 M3）。

顺手记下的**小债**（不阻塞，M1 时一并清理）：

- 无 README / 无测试 / 非 git 仓库（建议 git init 便于 diff 与回滚）。
- `cwd` 记在 config 里但工具还没用上；错误只 `console.error` 无分级。
- `model.ts` 默认模型名硬编码语义在 config 层，模型切换的「适配点」还没抽象。

---

## 3. 里程碑总览

| 里程碑 | 主题 | 一句话 | 交付时 Flux 能 | 大致规模 |
|--------|------|--------|----------------|----------|
| **M0** ✅ | 会说话 | 壳→脑→壳 | 单轮问答 | 已完成 |
| **M1** | 会动手（只读） | 循环 + 工具 | 自己翻代码回答「X 在哪被调、链路怎么走」 | ~2 个模块 + 测试 |
| **M2** | 会改码并自证 | 写/改 + 校验闭环 | 接下「改个功能并跑通编译/测试」 | ~2 个工具 + 安全策略 |
| **M3** | 记得住 | 跨会话记忆 | 今天告诉的约定，明天新会话主动遵守 | 1 模块 + 记忆文件约定 |
| **M4+** | 谈得来、办得长 | 对话式 + 长任务 | 追问纠偏、复杂目标拆步执行、中断续跑 | 探索性，见 §7 |

> M1–M3 是**主航道**（确定要做）；M4+ 是**跑道**（具体化成三条候选线，等 M1–M3 落地后按真实使用痛点选）。

---

## 4. 里程碑展开

### M1 — 会动手（只读）：主循环 + 只读工具

**目标**：一次提问可触发「模型思考 → 调用工具 → 拿到结果 → 再思考」的多轮循环，直到给出结论。此期工具**只读不写**，风险最小，先把循环和心智模型磨对。

**新增/改动模块（建议）**：

```
src/
  agent.ts      # M1 主循环：编排 model + registry + history（核心新增）
  tools/index.ts # 工具注册表：{ name → { schema, fn } }
  tools/fs.ts    # read_file / list_dir / glob / grep 等只读工具
  history.ts     # 消息历史（角色含 tool、截断策略）
  model.ts       # 改动：chat() 增加 tools 参数，支持把 tool 结果回传
  cli.ts         # 改薄：只做参数/错误出口，逻辑挪进 agent
```

**主循环伪码**（这是 Flux 的地基，M2/M3 都长在它上面）：

```ts
messages = [system, ...sessionHistory, user(input)]
for step in 1..MAX_STEPS:                    // 护栏：默认 8~12
  resp = model.chat(messages, tools=schemas) // 多脑可切只在这一个适配点
  messages.push(assistant(resp))
  if resp.toolCalls 为空: print(resp.text); return   // 模型已给出结论
  for call of resp.toolCalls:
    result = registry.run(call.name, call.args)      // try/catch，异常也返回结构体
    messages.push(tool(result, toolCallId=call.id))  // 按 id 回填，兼容各家协议
  // 每步把「正在做什么」打到 stderr，结论才走 stdout
```

**首批工具清单**（本地读代码最小集）：

| 工具 | 关键参数 | 说明 |
|------|---------|------|
| `read_file` | path, offset?, limit? | 读文件/带行范围（大文件分段） |
| `list_dir` | path? | 列目录 |
| `glob` | pattern | 按路径模式找文件 |
| `grep` | pattern, path?, glob? | 搜代码（不含 node_modules/dist） |
| `get_cwd` | — | 明确当前工作目录 |

**护栏（D5）**：

- `MAX_STEPS` 上限 + 单轮 tool 输出**截断**（如 >4000 字取头尾），防跑飞/防刷屏。
- 工具抛错 → 以 `tool(错误信息)` 回填让模型自愈换法，**不崩循环**。
- 未知/参数非法工具 → 结构化错误回填，让模型修正后重试。
- 进度与最终结论分离输出（中间过程 stderr / 结论 stdout），体感干净。

**验收（DoD）**：在 Flux 自己身上狗粮化——
`flux "src 下 cli.ts 的 main() 调了哪些模块？model.ts 的 chat() 怎么把回复传回 cli.ts？把链路讲清楚"` → Flux 自己读 4 个文件给出链路，且过程中至少真实调用 2 个只读工具。

**测试**：核心（循环、回填、截断、错误自愈）用 **stub 模型** 单测，确定性驱动；端到端少量真实 qwen 用例；手写冒烟脚本。

---

### M2 — 会改码并自证：写/改 + 校验闭环

**目标**：在 M1 循环上加入「写文件 / 改文件 / 跑命令」工具，并让 Flux **改完自己验证**。这是它从「咨询助手」变「干活助手」的一跳，安全策略同步上线。

**新增能力**：

- 工具：`write_file`（整文件）、`edit_file`（精确 old→new 替换，可限次数）、`run_shell`（含编译/测试）；改前自动**备份**到 `.flux/undo/`，支持撤销。
- **校验闭环**：对常见工程自动「改完跑一遍」——Node 项目 `tsc/build`，Python `pytest` 等；结果回填给模型，失败则继续修，直到绿或主动认怂。
- **统一 diff 报告**：Flux 汇报「我改了哪些文件、diff 长啥样」，让人一眼可审。

**安全确认策略（先按个人工具默认，见 §8 待定项）**：

| 操作 | 默认 | 备注 |
|------|------|------|
| 只读工具（M1） | 直接执行 | — |
| `write/edit` | 改前展示 diff，回车确认 | 可用 `--yes` 一次性放开 |
| `run_shell`（只读命令） | 直接执行 | `grep/tsc` 等 |
| `run_shell`（写/危险命令） | 确认 | `rm/curl 到外部/发布` 等 |

**验收（DoD）**：`flux "给 src/cli.ts 加一个 --verbose 开关：打开时把当前用的模型名打到 stderr；跑 npm run build 确认类型通过"` → Flux 读代码、改文件、自己跑 build，返回 diff，代码确实可用。

---

### M3 — 记得住：跨会话记忆

**目标**：对应参考注释里预留的「M3 在 system 末尾追加记忆」。让约定/进展跨会话存活，不靠每次重新交代。

**两级记忆**：

| 级别 | 位置 | 存什么 | 谁维护 |
|------|------|--------|--------|
| 项目记忆 | `$cwd/.flux/memory.md` | 本仓库约定/结构/进行中状态 | Flux 自动 + 人可手改 |
| 用户偏好 | `~/.flux/prefs.md` | 跨项目偏好（语言/风格/默认行为） | Flux 自动 + 人可手改 |

**写入时机**：会话结束时，由模型从本次对话**提炼「值得记的少量条目」**追加（克制，避免越记越臃肿）；用户也可显式说「记住：…」。

**注入与截断**：`buildSystemPrompt` 末尾拼入记忆条目；超长则取最近 N 条 / 摘要化，防止人设膨胀（记忆是给模型的上下文，不是归档库）。

**验收（DoD）**：今天告诉 Flux「本项目工具相关的注释用中文写」，结束会话；第二天新会话让它干活，它能主动遵守并指出「这是你告诉我的约定」。

---

## 5. 横切关注点（每个里程碑都守）

- **多脑可切（D1）**：`model`/`baseUrl` 全可配；「协议适配点」收敛在 `model.ts` 一处；工具 schema 只用 OpenAI function-calling 标准，避免绑死某家方言。
- **成本护栏（D5）**：步数上限、单轮输出截断、记忆有界。默认 qwen3-coder-plus 本身就便宜，但护栏要内建而不是靠自觉。
- **测试**：循环/解析类逻辑一律 **stub 模型单测**（确定、免钱、快）；每里程碑配 1–2 个端到端狗粮用例。
- **UX 与错误**：进度(stderr)与结论(stdout)分离；错误分「配置错误/模型错误/工具错误」给可操作提示。
- **目录约定**：项目态数据统一放 `.flux/`（undo、memory、session 快照），用 gitignore 语义处理。

---

## 6. 待定决策点（不阻塞 M1，先列着，随时拍板）

| # | 问题 | 影响 | 默认建议 |
|---|------|------|---------|
| O1 | Flux 与 Claude Code 最终什么关系（替代/共存/观望） | 决定 M4+ 是否做交互式 TUI、是否必须国产模型 | 先观望，Flux 先证明「国产脑 + 自研壳」够不够好 |
| O2 | 安全确认模型 | M2 交互形态 | 改前确认 + `--yes` 逃生门（个人工具默认） |
| O3 | 交互形态 | M4+ 范围 | 先保持单次调用 + stdin，够用再加对话 |
| O4 | 要不要 git init + 版本管理 | 全部 | 建议 init（diff/回滚是改码工具的前提） |

---

## 7. M4+ 跑道（探索性，等 M1–M3 落定按痛点选）

1. **对话式 CLI**：单次调用 → 可追问/纠偏的多轮会话；输出流式（逐字出更跟手）。
2. **长任务执行**：复杂目标先「拆计划再分步跑」，checkpoint 存 `.flux/sessions/`，中断可续。
3. **扩展触点**：若 O1 走向「替代」，按需挂云端工具（日志/DB/发布）——届时可复用你在用的一套阿里云/图库 MCP 的思路，但那是另一个周期的事。

---

## 8. 下一步（M1 开工清单）

1. `git init` + `.gitignore`（node_modules/dist/.flux）+ 首次 commit（顺带解决 O4）。
2. `model.ts`：`chat()` 支持 `tools` 参数 + tool 结果回填。
3. 新增 `tools/index.ts` + `tools/fs.ts`（5 个只读工具）+ `history.ts`。
4. 新增 `agent.ts` 主循环（护栏内置），`cli.ts` 改薄接入。
5. stub 模型单测 + 冒烟脚本 + 一个端到端狗粮用例（= M1 DoD）。
6. 顺手补 README 与 M1 段落的工程化小债。

> 开始 M1 前建议把 §6 的 O2（安全确认）至少粗定一版，其余可后置。
